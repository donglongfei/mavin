version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: mavin-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    networks:
      - mavin-network
    restart: unless-stopped

volumes:
  qdrant_storage:

networks:
  mavin-network:
    driver: bridge
